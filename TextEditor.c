#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

void main() {
    // para possibilitar o uso de acentos
    setlocale(LC_ALL, "");

    // criar variaveis
    char input, texto;
    input = ' ';

    // recebe os comandos e chama as funcoes correspondentes
    while (input != '!') {
        
    }
}